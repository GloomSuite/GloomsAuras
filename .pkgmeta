# BigWigs packager config. Publishes to GitHub Releases (same model as GloomsBuildBarn).
# WoWUp installs + auto-updates from those releases via the repo URL.

package-as: GloomsAuras

enable-nolib-creation: no

# Embedded libraries. The packager fetches these into Libs/ at release build time
# only — they are NOT committed to the repo (kept clean). Locally, Libs/ is present
# in the working copy so everything works. LDB + LibDBIcon dropped with Phase D —
# the minimap launcher (and those libs) moved to GloomsHub, the hard dependency.
externals:
  Libs/LibStub: https://repos.wowace.com/wow/libstub/trunk
  Libs/CallbackHandler-1.0: https://repos.wowace.com/wow/callbackhandler/trunk/CallbackHandler-1.0
  Libs/LibSharedMedia-3.0: https://repos.wowace.com/wow/libsharedmedia-3-0/trunk/LibSharedMedia-3.0
  # Phase G, 2026-07-24: the old wowace SVN URL (wow/libcustomglow-1-0/trunk)
  # 404s — the lib is not hosted there. Verified live home; its repo root holds
  # exactly what Libs/LibCustomGlow-1.0 holds locally (.lua/.toc/.xml/LICENSE).
  Libs/LibCustomGlow-1.0: https://github.com/Stanzilla/LibCustomGlow

# Keep repo-management files out of the packaged addon zip.
ignore:
  - README.md
  - CLAUDE.md
  - .claude
  - .github
  - .gitignore
  - .pkgmeta
  - docs
